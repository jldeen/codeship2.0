- name: Azure ACS Deploy
  service: acsdeploy
  command: bash /deploy/deployment/kubernetes/acs_deploy.sh
  # tag: master
- type: serial
  name: SSH Tunnel
  service: sshtunnel
  steps:
  - command: kubectl get nodes
  # tag: master